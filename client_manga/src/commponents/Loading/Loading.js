import React from "react";
import "./Loading.scss";

const Loading = () => {
  return (
    <div className="container_loading">
      <div className="spinner" />
    </div>
  );
};

export default Loading;
